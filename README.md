# LockProject
@'


MMO RP football japonais sur nanos world.

## Structure

- `nanos-server/` — serveur nanos + packages Lua
- `ue5-adk/` — projet UE5 (ADK pour assets/maps)
- `blender-sources/` — fichiers .blend bruts
- `docs/` — GDD, level design, tech stack, conventions
- `scripts/` — utilitaires (deploy, backup, scaffolding)

## Conventions

Voir `docs/conventions-code.md`.

## Roadmap

Voir `docs/roadmap-mvp.md`. Phase actuelle : 0 (setup).
'@ | Out-File -FilePath README.md -Encoding utf8 -NoNewline